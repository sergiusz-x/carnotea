# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
out/
.next/
.nuxt/
.svelte-kit/
.vite/

# Turborepo
.turbo/

# Caches
.cache/
.eslintcache
.prettiercache
.parcel-cache/
node-compile-cache/

# Coverage
coverage/
*.lcov
.nyc_output/

# Test artifacts
playwright-report/
test-results/
playwright/.cache/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Env files
.env*
!.env.example

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# TS build info
*.tsbuildinfo

# Drizzle / DB local artifacts
drizzle/meta/_journal.local.json

# Claude Code local settings + worktrees (per-developer, not shared)
.claude/settings.local.json
.claude/settings.json
.claude/worktrees/

# work-ticket agent worktrees (per-developer, not shared) — also excluded
# from ESLint's root scan below, since a nested worktree's own node_modules
# isn't guaranteed to resolve the same way as the parent's.
.worktrees/

# agent-browser — browser session data (per-developer)
.agent-browser/

# Generated single-file SQL dist (legacy)
dist/
